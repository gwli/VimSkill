Vim 编程
********

vim 有自身的脚本语言，同时也支持其他常见的语言，例如 python, tcl,perl等等。

Vim shell
=========

如何加载脚本 :command:`:so[ource]  scripts.vim` 就像bash 中的 source 命令一样的。

这样就可以用 vim来代替 sed,与awk.


Vim 编程模型
============

寄存器
======

宏录制
======



